# streamlit_app.py

import streamlit as st

# from power_automate_connection import DuckDBConnection

st.set_page_config(
    page_title="Hello",
    page_icon="👋",
)

# conn = st.experimental_connection("api", type=DuckDBConnection)
# conn.add_data({"id": 1, "name": "priya"})